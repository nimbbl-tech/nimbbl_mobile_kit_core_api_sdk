# Production Environment Configuration
# Copy this to gradle.properties to use production settings

# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# Event Logging Configuration - Now dynamically determined based on BASE_URL
# EVENT_LOG_URL and DEFAULT_TENANT_ID are computed at runtime based on the BASE_URL

# Other production-specific settings can be added here

org.gradle.java.home=/opt/homebrew/Cellar/openjdk@17/17.0.16/libexec/openjdk.jdk/Contents/Home
